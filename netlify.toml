[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# Route côté client (React Router) : toute URL renvoie vers index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
